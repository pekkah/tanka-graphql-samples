apiVersion: v1
kind: Service
metadata:
  name: {{ template "messageshost.fullname" . }}
  labels:
    app: {{ template "messageshost.name" . }}
    chart: {{ template "messageshost.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "messageshost.name" . }}
    release: {{ .Release.Name }}
