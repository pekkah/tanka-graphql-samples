apiVersion: v1
kind: Service
metadata:
  name: {{ template "channelshost.fullname" . }}
  labels:
    app: {{ template "channelshost.name" . }}
    chart: {{ template "channelshost.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "channelshost.name" . }}
    release: {{ .Release.Name }}
