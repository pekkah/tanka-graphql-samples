apiVersion: v1
kind: Service
metadata:
  name: {{ template "host.fullname" . }}
  labels:
    app: {{ template "host.name" . }}
    chart: {{ template "host.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "host.name" . }}
    release: {{ .Release.Name }}
