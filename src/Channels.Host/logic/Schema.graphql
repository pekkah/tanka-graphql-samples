directive @gen(
	asAbstract: Boolean!
) on FIELD_DEFINITION

type Channel {
    id: ID!
	name: String!
}

type Query {
	channels: [Channel!]! @gen(asAbstract: true)
	channel(channelId: ID!): Channel
}

schema {
    query: Query
}