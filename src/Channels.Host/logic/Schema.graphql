directive @gen(
	asAbstract: Boolean! = false,
	asProperty: Boolean! = false,
	clrType: String = null
) on FIELD_DEFINITION

type Channel {
    id: Int!
	name: String!
}

type Query {
	channels: [Channel!]!
	channel(channelId: Int!): Channel
}

schema {
    query: Query
}