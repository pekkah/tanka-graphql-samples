directive @gen(
	asAbstract: Boolean!
) on FIELD_DEFINITION

type Channel {
    id: Int!
	name: String!
}

type Query {
	channels: [Channel!]!
	channel(channelId: Int!): Channel
}

schema {
    query: Query
}